Замечание подтверждено: `syncMenu` пока не запускает импорт, а адаптеров iiko/r_keeper и обработчика импорта в каталог Bonapp в проекте нет. Реализация фактической синхронизации остаётся блокирующей; endpoint не сообщает об успешном запуске. Тред оставляю открытым.
