Проверил реализацию `syncMenu` и поискал существующий процесс импорта в `apps/api/src` и `packages`: POS-адаптеров и обработчика задания в репозитории нет. В доступных требованиях также не заданы API-контракты провайдеров и правила сопоставления их меню с каталогом Bonapp. Поэтому `202 Accepted` без фактического запуска импорта был бы ложным подтверждением; блокирующее замечание остаётся открытым до определения этих контрактов и реализации процесса.
