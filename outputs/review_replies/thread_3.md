Перенёс отправку `order:created` и `order:updated` за пределы callback `transactionForTenant`: событие уходит только после успешного завершения транзакции. Добавил регрессионную проверку порядка commit и публикации.
