PORT=3000
CORS_ORIGIN=http://localhost:5173

# Comma-separated trusted reverse proxy IPs/CIDRs (for example: 10.0.0.0/8).
# Leave empty when the API is not behind a reverse proxy. Never use a hop count;
# ensure network policy prevents direct public access when proxies are trusted.
TRUSTED_PROXY_ADDRESSES=

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bonapp
REDIS_URL=redis://localhost:6379

JWT_SECRET=replace-with-a-long-random-secret
JWT_REFRESH_SECRET=replace-with-a-different-long-random-secret

# Generate a random 32-byte key and encode it as 64 hexadecimal characters.
# Example: openssl rand -hex 32
TOTP_ENCRYPTION_KEY=replace-with-64-hex-character-random-key

# Payment provider will be selected in a separate task.
PAYMENT_PUBLIC_KEY=replace-with-payment-public-key
PAYMENT_SECRET_KEY=replace-with-payment-secret-key

# S3-compatible object storage (MinIO for local dev, S3-compatible for production)
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=bonapp
# Public-facing base URL for stored objects (same as S3_ENDPOINT for MinIO)
S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
